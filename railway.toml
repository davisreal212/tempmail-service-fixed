[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'uvicorn api:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
